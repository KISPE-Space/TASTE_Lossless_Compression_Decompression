/* File generated by kazoo for TASTE - do not edit */
#include <string.h>
#include <assert.h>

#include <rtems.h>

#include "partition_1_interface.h"
#include "transport.h"
#include "routing.h"


#include "testcompressuncompress_vm_if.h"

void call_testcompressuncompress_runonce (const char* buf, size_t len)
{
    extern rtems_id testcompressuncompress_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(testcompressuncompress_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT);
    assert(result == RTEMS_SUCCESSFUL);

    testcompressuncompress_RunOnce();
    rtems_semaphore_release(testcompressuncompress_Semaphore);
}
#include "testcompressuncompress_vm_if.h"

void call_testcompressuncompress_samrh71rx (const char* buf, size_t len)
{
    extern rtems_id testcompressuncompress_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(testcompressuncompress_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT);
    assert(result == RTEMS_SUCCESSFUL);

    testcompressuncompress_samrh71rx(buf, len);
    rtems_semaphore_release(testcompressuncompress_Semaphore);
}

#include "datacompressor_vm_if.h"


void call_protected_DataCompressor_ClearBuffer(void)
{
    extern rtems_id datacompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datacompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datacompressor_ClearBuffer();
    rtems_semaphore_release(datacompressor_Semaphore);
}
void call_protected_DataCompressor_ReadData
      (char *OUT_buf_dataout, size_t *size_OUT_buf_dataout,
       const char *IN_buf_byteoffset, size_t size_IN_buf_byteoffset,
       const char *IN_buf_bytestoread, size_t size_IN_buf_bytestoread,
       char *OUT_buf_byteswritten, size_t *size_OUT_buf_byteswritten)

{
    extern rtems_id datacompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datacompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datacompressor_ReadData
      (OUT_buf_dataout, size_OUT_buf_dataout,
       IN_buf_byteoffset, size_IN_buf_byteoffset,
       IN_buf_bytestoread, size_IN_buf_bytestoread,
       OUT_buf_byteswritten, size_OUT_buf_byteswritten);

    rtems_semaphore_release(datacompressor_Semaphore);
}
void call_protected_DataCompressor_StartCompression(void)
{
    extern rtems_id datacompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datacompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datacompressor_StartCompression();
    rtems_semaphore_release(datacompressor_Semaphore);
}
void call_protected_DataCompressor_Status
      (char *OUT_buf_currentstatus, size_t *size_OUT_buf_currentstatus,
       char *OUT_buf_compresseddatalength, size_t *size_OUT_buf_compresseddatalength)

{
    extern rtems_id datacompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datacompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datacompressor_Status
      (OUT_buf_currentstatus, size_OUT_buf_currentstatus,
       OUT_buf_compresseddatalength, size_OUT_buf_compresseddatalength);

    rtems_semaphore_release(datacompressor_Semaphore);
}
void call_protected_DataCompressor_WriteBuffer
      (const char *IN_buf_databuffer, size_t size_IN_buf_databuffer,
       const char *IN_buf_datalength, size_t size_IN_buf_datalength)

{
    extern rtems_id datacompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datacompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datacompressor_WriteBuffer
      (IN_buf_databuffer, size_IN_buf_databuffer,
       IN_buf_datalength, size_IN_buf_datalength);

    rtems_semaphore_release(datacompressor_Semaphore);
}
#include "datauncompressor_vm_if.h"


void call_protected_DataUncompressor_ClearBuffer(void)
{
    extern rtems_id datauncompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datauncompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datauncompressor_ClearBuffer();
    rtems_semaphore_release(datauncompressor_Semaphore);
}
void call_protected_DataUncompressor_ReadData
      (char *OUT_buf_dataout, size_t *size_OUT_buf_dataout,
       const char *IN_buf_byteoffset, size_t size_IN_buf_byteoffset,
       const char *IN_buf_bytestoread, size_t size_IN_buf_bytestoread,
       char *OUT_buf_byteswritten, size_t *size_OUT_buf_byteswritten)

{
    extern rtems_id datauncompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datauncompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datauncompressor_ReadData
      (OUT_buf_dataout, size_OUT_buf_dataout,
       IN_buf_byteoffset, size_IN_buf_byteoffset,
       IN_buf_bytestoread, size_IN_buf_bytestoread,
       OUT_buf_byteswritten, size_OUT_buf_byteswritten);

    rtems_semaphore_release(datauncompressor_Semaphore);
}
void call_protected_DataUncompressor_StartUncompression(void)
{
    extern rtems_id datauncompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datauncompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datauncompressor_StartUncompression();
    rtems_semaphore_release(datauncompressor_Semaphore);
}
void call_protected_DataUncompressor_Status
      (char *OUT_buf_currentstatus, size_t *size_OUT_buf_currentstatus,
       char *OUT_buf_decompresseddatalength, size_t *size_OUT_buf_decompresseddatalength)

{
    extern rtems_id datauncompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datauncompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datauncompressor_Status
      (OUT_buf_currentstatus, size_OUT_buf_currentstatus,
       OUT_buf_decompresseddatalength, size_OUT_buf_decompresseddatalength);

    rtems_semaphore_release(datauncompressor_Semaphore);
}
void call_protected_DataUncompressor_WriteBuffer
      (const char *IN_buf_databuffer, size_t size_IN_buf_databuffer,
       const char *IN_buf_datalength, size_t size_IN_buf_datalength)

{
    extern rtems_id datauncompressor_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(datauncompressor_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    datauncompressor_WriteBuffer
      (IN_buf_databuffer, size_IN_buf_databuffer,
       IN_buf_datalength, size_IN_buf_datalength);

    rtems_semaphore_release(datauncompressor_Semaphore);
}
// Required interfaces of function TestCompressUncompress:

// Required interface Comp_ClearBuffer in function TestCompressUncompress
#include "datacompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_comp_clearbuffer(asn1SccPID dest_pid)
{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_comp_clearbuffer_routing_table[PID_datacompressor]) || PID_datacompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datacompressor == dest_pid)
    #endif
    {
        datacompressor_clearbuffer_sender_pid = PID_testcompressuncompress;
    call_protected_DataCompressor_ClearBuffer();
    }
}
// Required interface Comp_ReadData in function TestCompressUncompress
#include "datacompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_comp_readdata
        (asn1SccPID dest_pid,
         char *OUT_buf_dataout, size_t *size_OUT_buf_dataout,
         const char *IN_buf_byteoffset, size_t size_IN_buf_byteoffset,
         const char *IN_buf_bytestoread, size_t size_IN_buf_bytestoread,
         char *OUT_buf_byteswritten, size_t *size_OUT_buf_byteswritten)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_comp_readdata_routing_table[PID_datacompressor]) || PID_datacompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datacompressor == dest_pid)
    #endif
    {
        datacompressor_readdata_sender_pid = PID_testcompressuncompress;
    call_protected_DataCompressor_ReadData
         (OUT_buf_dataout, size_OUT_buf_dataout,
         IN_buf_byteoffset, size_IN_buf_byteoffset,
         IN_buf_bytestoread, size_IN_buf_bytestoread,
         OUT_buf_byteswritten, size_OUT_buf_byteswritten);

    }
}
// Required interface Comp_StartCompression in function TestCompressUncompress
#include "datacompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_comp_startcompression(asn1SccPID dest_pid)
{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_comp_startcompression_routing_table[PID_datacompressor]) || PID_datacompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datacompressor == dest_pid)
    #endif
    {
        datacompressor_startcompression_sender_pid = PID_testcompressuncompress;
    call_protected_DataCompressor_StartCompression();
    }
}
// Required interface Comp_Status in function TestCompressUncompress
#include "datacompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_comp_status
        (asn1SccPID dest_pid,
         char *OUT_buf_currentstatus, size_t *size_OUT_buf_currentstatus,
         char *OUT_buf_compresseddatalength, size_t *size_OUT_buf_compresseddatalength)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_comp_status_routing_table[PID_datacompressor]) || PID_datacompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datacompressor == dest_pid)
    #endif
    {
        datacompressor_status_sender_pid = PID_testcompressuncompress;
    call_protected_DataCompressor_Status
         (OUT_buf_currentstatus, size_OUT_buf_currentstatus,
         OUT_buf_compresseddatalength, size_OUT_buf_compresseddatalength);

    }
}
// Required interface Comp_WriteBuffer in function TestCompressUncompress
#include "datacompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_comp_writebuffer
        (asn1SccPID dest_pid,
         const char *IN_buf_databuffer, size_t size_IN_buf_databuffer,
         const char *IN_buf_datalength, size_t size_IN_buf_datalength)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_comp_writebuffer_routing_table[PID_datacompressor]) || PID_datacompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datacompressor == dest_pid)
    #endif
    {
        datacompressor_writebuffer_sender_pid = PID_testcompressuncompress;
    call_protected_DataCompressor_WriteBuffer
         (IN_buf_databuffer, size_IN_buf_databuffer,
         IN_buf_datalength, size_IN_buf_datalength);

    }
}
// Required interface Uncomp_ClearBuffer in function TestCompressUncompress
#include "datauncompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_uncomp_clearbuffer(asn1SccPID dest_pid)
{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_uncomp_clearbuffer_routing_table[PID_datauncompressor]) || PID_datauncompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datauncompressor == dest_pid)
    #endif
    {
        datauncompressor_clearbuffer_sender_pid = PID_testcompressuncompress;
    call_protected_DataUncompressor_ClearBuffer();
    }
}
// Required interface Uncomp_ReadData in function TestCompressUncompress
#include "datauncompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_uncomp_readdata
        (asn1SccPID dest_pid,
         char *OUT_buf_dataout, size_t *size_OUT_buf_dataout,
         const char *IN_buf_byteoffset, size_t size_IN_buf_byteoffset,
         const char *IN_buf_bytestoread, size_t size_IN_buf_bytestoread,
         char *OUT_buf_byteswritten, size_t *size_OUT_buf_byteswritten)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_uncomp_readdata_routing_table[PID_datauncompressor]) || PID_datauncompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datauncompressor == dest_pid)
    #endif
    {
        datauncompressor_readdata_sender_pid = PID_testcompressuncompress;
    call_protected_DataUncompressor_ReadData
         (OUT_buf_dataout, size_OUT_buf_dataout,
         IN_buf_byteoffset, size_IN_buf_byteoffset,
         IN_buf_bytestoread, size_IN_buf_bytestoread,
         OUT_buf_byteswritten, size_OUT_buf_byteswritten);

    }
}
// Required interface Uncomp_StartUncompression in function TestCompressUncompress
#include "datauncompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_uncomp_startuncompression(asn1SccPID dest_pid)
{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_uncomp_startuncompression_routing_table[PID_datauncompressor]) || PID_datauncompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datauncompressor == dest_pid)
    #endif
    {
        datauncompressor_startuncompression_sender_pid = PID_testcompressuncompress;
    call_protected_DataUncompressor_StartUncompression();
    }
}
// Required interface Uncomp_Status in function TestCompressUncompress
#include "datauncompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_uncomp_status
        (asn1SccPID dest_pid,
         char *OUT_buf_currentstatus, size_t *size_OUT_buf_currentstatus,
         char *OUT_buf_decompresseddatalength, size_t *size_OUT_buf_decompresseddatalength)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_uncomp_status_routing_table[PID_datauncompressor]) || PID_datauncompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datauncompressor == dest_pid)
    #endif
    {
        datauncompressor_status_sender_pid = PID_testcompressuncompress;
    call_protected_DataUncompressor_Status
         (OUT_buf_currentstatus, size_OUT_buf_currentstatus,
         OUT_buf_decompresseddatalength, size_OUT_buf_decompresseddatalength);

    }
}
// Required interface Uncomp_WriteBuffer in function TestCompressUncompress
#include "datauncompressor_vm_if.h"  // Remote language: C
void vm_testcompressuncompress_uncomp_writebuffer
        (asn1SccPID dest_pid,
         const char *IN_buf_databuffer, size_t size_IN_buf_databuffer,
         const char *IN_buf_datalength, size_t size_IN_buf_datalength)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_uncomp_writebuffer_routing_table[PID_datauncompressor]) || PID_datauncompressor == dest_pid)
    #else
    if (PID_env == dest_pid || PID_datauncompressor == dest_pid)
    #endif
    {
        datauncompressor_writebuffer_sender_pid = PID_testcompressuncompress;
    call_protected_DataUncompressor_WriteBuffer
         (IN_buf_databuffer, size_IN_buf_databuffer,
         IN_buf_datalength, size_IN_buf_datalength);

    }
}
// Required interface samrh71tx in function TestCompressUncompress
void vm_testcompressuncompress_samrh71tx
        (asn1SccPID dest_pid,
         const char *IN_buf_ibyteout, size_t size_IN_buf_ibyteout)

{
    #if defined USE_ROUTING_TABLE
    if ((PID_env == dest_pid && testcompressuncompress_samrh71tx_routing_table[PID_uartotherend]) || PID_uartotherend == dest_pid)
    #else
    if (PID_env == dest_pid || PID_uartotherend == dest_pid)
    #endif
    {
        deliver_to_uartotherend_samrh71tx(PID_testcompressuncompress, (const uint8_t*)(IN_buf_ibyteout), size_IN_buf_ibyteout);
    }
}
#include "testcompressuncompress_vm_if.h"


void call_protected_TestCompressUncompress_RunOnce(void)
{
    extern rtems_id testcompressuncompress_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(testcompressuncompress_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    testcompressuncompress_RunOnce();
    rtems_semaphore_release(testcompressuncompress_Semaphore);
}
void call_protected_TestCompressUncompress_samrh71rx
      (const char *IN_buf_ibytein, size_t size_IN_buf_ibytein)

{
    extern rtems_id testcompressuncompress_Semaphore;

    rtems_status_code result = rtems_semaphore_obtain(testcompressuncompress_Semaphore, RTEMS_WAIT,
                                                      RTEMS_NO_TIMEOUT );
    assert(result == RTEMS_SUCCESSFUL);

    testcompressuncompress_samrh71rx
      (IN_buf_ibytein, size_IN_buf_ibytein);

    rtems_semaphore_release(testcompressuncompress_Semaphore);
}
